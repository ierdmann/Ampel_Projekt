import time												# t muss noch als Zahl definiert werden
n = int(time.time())
def Zeit():
	while int(time.time())-n <= t and schalter.value != True:
		
		print("Es sind", int(time.time())-n, "Sekunden vergangen!")
		

		if int(time.time())-n >= t:		# Zeit wird verglichen 
			print("Nun sind schon", int(time.time())-n, "Sekunden vergangen")